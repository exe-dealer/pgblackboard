#!/bin/sh -e

cd "$( dirname "$( realpath "$0" )" )"

exec deno run \
  --allow-read=../ui \
  --allow-net \
  --v8-flags=--stack_trace_limit=30 \
  --no-config \
  --no-remote \
  --no-npm \
  ../server/pgbb.js "$@"
