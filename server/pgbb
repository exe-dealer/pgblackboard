#!/bin/sh -e

cd "$( dirname "$( realpath "$0" )" )"

exec deno run \
  --no-prompt \
  --no-config \
  --no-lock \
  --no-remote \
  --no-npm \
  --allow-net \
  --allow-read=../ui \
  --unstable-raw-imports \
  ./pgbb.js "$@"
